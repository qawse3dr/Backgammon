using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/**
 * This class just needs to hold the BoardIndex so on collision
 * We are able to find which box we are colliding with
 */
public class BoardHitBox : MonoBehaviour {
  public int BoardIndex;
}
